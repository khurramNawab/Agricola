# Base URL of the agri-backend API (no trailing slash).
# Local dev:   http://localhost:5000/api/v1
# Production:  https://api.agricola.co.in/api/v1  (committed in .env.production, used by `vite build`)
## CHANGE
# LOCSL
VITE_API_URL=http://localhost:5000/
